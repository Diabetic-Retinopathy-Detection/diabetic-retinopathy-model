patch_size: 16
embed_dim: 768
depth: 12
num_heads: 12
mlp_ratio: 4.0
drop_rate: 0.0
attn_drop_rate: 0.0

pretrained_weights: null
num_classes: 5

dim: 256
mlp_dim: 4096
temperature: 1.0
saliency_threshold: 0.25
pool_mode: max

data_dir: null
image_size: [224, 224]
input_size: 224
data_index_path: data_index/pretraining_dataset.pkl
dataset_ratio: 1.0
num_workers: 4
train_split: 0.8
val_split: 0.1

batch_size: 32
learning_rate: 0.0001
max_epochs: 100
gradient_clip_val: 1.0
accumulate_grad_batches: 1
precision: 16-mixed
warmup_epochs: 40
momentum_base: 0.99
momentum_max: 1.0
lambda_c: 1.0
lambda_s: 10.0
ss_decay: false
save_every: 20

host: 0.0.0.0
port: 8000
workers: 1
max_batch_size: 16
