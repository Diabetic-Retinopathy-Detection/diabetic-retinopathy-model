# TODO: fill in for fine‑tuning phase
model:
  name: vit_base
  pretrained_weights: null
  num_classes: 5

data:
  data_dir: null
  image_size: [224, 224]
  train_split: 0.8
  val_split: 0.1

training:
  batch_size: 32
  learning_rate: 1e-05
  max_epochs: 50
  gradient_clip_val: 1.0
  accumulate_grad_batches: 1
  precision: 16-mixed
  num_workers: 4

serving:
  host: 0.0.0.0
  port: 8000
  workers: 1
  max_batch_size: 16
